@model IEnumerable<E_Lect.Models.PostImage>

@{
    ViewBag.Title = "AdminViewIndex";
}

<h2>AdminViewIndex</h2>


<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.Image)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImagePath)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.Image)
        </td>*@
        <td>
            <img src="@Url.Action("RenderImage2","PostImages",new { id = item.PostImageID})" width="80" height="50" />
        </td>
        <td>
            @Html.ActionLink("Validate Image", "ValidateImages", new { id=item.PostImageID }) |
           
            @Html.ActionLink("Delete", "Delete", new { id=item.PostImageID })
        </td>
    </tr>
}
    @Html.ActionLink("Back", "AdminHome","Home");

</table>
